var through = require('through2')
  , _ = require('lodash')

module.exports = function(config) {
  return through.obj(function(file, enc, next) {
    console.log(file.relative, JSON.stringify(_.omit(file, 'ast', '_contents'), null, 2))
    this.push(file)
    next()
  })
}

