var through = require('through2')
  , _ = require('lodash')
  , colors = require('colors')

module.exports = function(config) {
  return through.obj(function(file, enc, next) {
    console.log('---------- SPY -----------'.green)
    console.log(JSON.stringify(_.omit(file, 'ast', '_contents'), null, 2))
    console.log('--------------------------'.green)
    this.push(file)
    next()
  })
}

