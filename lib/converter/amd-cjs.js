// if(body.length === 1) {
//   var amd = _.partial(ast.is.amd, _, body[0])
//     , args = amd('define') || amd('require') ? func(body).args : null
//
//   hasRequire = args && args.indexOf('require') > -1
//   deps = getDeps(body)
// }
//

