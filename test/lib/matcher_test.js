var matcher = require('../../lib/matcher')

exports['matcher'] = {
  setUp: function(done) {
    done();
  },
  'init': function(test) {
    // test.ok(false, "TODO implement")
    test.done();
  },
};
